# wrangler.toml — Cloudflare Pages configuration
# Optional: only needed if using wrangler CLI to deploy

name = "pinsave"
compatibility_date = "2024-01-01"

[env.production]
name = "pinsave"

# Pages config
pages_build_output_dir = "."
